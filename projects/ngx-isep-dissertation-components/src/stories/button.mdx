import { Canvas, Meta } from '@storybook/blocks';
 
import * as ButtonStories from './button.stories';
 
<Meta of={ButtonStories} />
 
# Button

The `ButtonComponent` is a customizable button element with different colors, sizes, and icons. It also supports a disabled state.

<Canvas of={ButtonStories.Default} />

## Props

- `color`: The color of the button. Options are available through the ButtonColor enum: `BLACK`, `WHITE`, `CORAL`.
- `size`: The size of the button. Options are available through the ButtonSize enum: `SMALL`, `MEDIUM`, `LARGE`.
- `icon`: The icon to display inside the button.
- `label`: The text label of the button.
- `disabled`: Boolean to disable the button.

## Usage

```html
<isep-lib-button
  [color]="ButtonColor.BLACK"
  [size]="ButtonSize.MEDIUM"
  [label]="'Button'"
  [disabled]="false"
></isep-lib-button>
```
To use `<isep-lib-button>` add the respective module to project module imports:

```typescript
// app.module.ts
import { ButtonModule } from 'ngx-dissertation-components-lib';

@NgModule({ 
	imports: [ButtonModule]
})
export class AppModule {}
```

### Examples
- [Library Code GIT](https://github.com/carinaalasisep/ngx-dissertation-components-lib/tree/main/projects/ngx-isep-dissertation-components/src/lib/button)
- [MFE example Code GIT](https://github.com/carinaalasisep/dissertation-productcat-mfe/blob/main/src/app/features/product-details/product-details.component.html)

 
